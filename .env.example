# LDAP Proxy Test Suite — Environment Configuration
#
# Copy this file to .env and edit as needed:
#   cp .env.example .env
#
# All values have sensible defaults in docker-compose.yml, so .env is optional.
# Environment variables set in the shell override both .env and compose defaults.

# LDAP proxy server URI (the VIP fronting your LDAP proxy)
LDAP_URI=ldap://192.168.10.116

# Search base DN
LDAP_BASE=DC=mydomain,DC=local

# Service account for nslcd bind (used for user DN lookups)
LDAP_BINDDN=CN=Administrator,CN=Users,DC=mydomain,DC=local
LDAP_BINDPW=Password123

